var path = require("path");

module.exports = {
    namespaces: {
        "test.test1": __dirname
    },
    bindings: {
        "test.binding": path.resolve(__dirname, "binding")
    },
    loads: [
        path.resolve(__dirname, "load")
    ],
    modules: [
        "autoloader-test-2"
    ]
};
