{
  "name": "violin-autoloader",
  "version": "1.0.1",
  "description": "Simple class autoloader for Node.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/violinjs/violin-autoloader.git"
  },
  "main": "src/Autoloader.js",
  "scripts": {
    "test": "mocha --harmony-proxies -r assert -r should -r should-sinon",
    "compile-tests": "cd ./test/use-case/binding; node-gyp clean configure build; cd ../../../node_modules/autoloader-test-1/binding; node-gyp clean configure build"
  },
  "author": "Jonathan ARNAULT <contact@jonathanarnault.com>",
  "license": "MIT",
  "devDependencies": {
    "mocha": "~2.3.0",
    "nan": "^2.1.0",
    "should": "~8.0.0",
    "should-sinon": "0.0.3",
    "sinon": "^1.17.2"
  },
  "engines": {
    "node": ">=4.0.0"
  }
}
